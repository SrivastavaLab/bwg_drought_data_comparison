packages:
  - ggplot2
  - dplyr
  - bwgtools
  - GGally
  - rmarkdown
  - knitr
  - xtable

sources:
  - Rscripts/offline.R
  
targets:
  all:
    depends:
      - reports
      - checks

  physical:
    command: combine_tab(sheetname = I("bromeliad.physical"))
    cleanup_level: purge


include:
  - remake_sites.yml
